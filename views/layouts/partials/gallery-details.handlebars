<section class="card">
  <a href="/gallery/{{id}}">
    <header>{{name}}</header>
    {{#if paintings.[0]}}
      <img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}">
    {{else}}
      <p>No painting available</p>
    {{/if}}
  </a>
</section>
